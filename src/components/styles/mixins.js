export const H1_style = `
  text-align: center;
  font-size: 3rem;
  font-weight: 900;
  border: 2px solid white;
  text-transform: uppercase;
  color: hsl(var(--purple));
  
`

export const H2_style = `
  text-align: center;
  font-size: 2rem;
  font-weight: 700;
  text-transform: uppercase;
  text-decoration: underline;
  color: hsl(var(--green));
`

export const H3_style = `
  text-align: center;
  font-size: 1.5rem;
  font-weight: 500;
  text-transform: uppercase;
  color: hsl(var(--green-hover));
`

export const P_style = `
  font-size: 1.25rem;
  line-height: 1;
  
`

/////////////////////////////////

export const HeadingL = `
  color: var(--marine-blue);
  font-size: 1.5rem;
  /* font-size: clamp(2.5rem, 1.62rem + 3.76vw, 5rem); */
  font-weight: 700;
`

export const HeadingM = `
  color: var(--marine-blue);
  font-size: 1rem;
  /* font-size: clamp(2.5rem, 1.62rem + 3.76vw, 5rem); */
  font-weight: 500;
`

export const HeadingS = `
  color: var(--marine-blue);
  font-size: 0.875rem;
  /* font-size: clamp(2.5rem, 1.62rem + 3.76vw, 5rem); */
  font-weight: 500;
`

export const IndexNo = `
  color: var(--white);
  font-size: 0.875rem;
  /* font-size: clamp(2.5rem, 1.62rem + 3.76vw, 5rem); */
  font-weight: 700;
`

export const BodyX = `
  color: var(--cool-grey);
  font-size: 1rem;
  /* font-size: clamp(2.5rem, 1.62rem + 3.76vw, 5rem); */
  font-weight: 400;
  line-height: 1.56;
`

export const InfoM = `
  color: var(--cool-grey);
  font-size: 0.875rem;
  /* font-size: clamp(2.5rem, 1.62rem + 3.76vw, 5rem); */
  font-weight: 400;
  line-height: 1.43;
`

export const InfoS = `
  color: var(--cool-grey);
  font-size: 0.75rem;
  /* font-size: clamp(2.5rem, 1.62rem + 3.76vw, 5rem); */
  font-weight: 400;
  line-height: 1.67;
`

export const CostL = `
  color: var(--purplish-blue);
  font-size: 1rem;
  /* font-size: clamp(2.5rem, 1.62rem + 3.76vw, 5rem); */
  font-weight: 700;
  line-height: 1.25;
`

export const CostS = `
  color: var(--purplish-blue);
  font-size: 0.75rem;
  /* font-size: clamp(2.5rem, 1.62rem + 3.76vw, 5rem); */
  font-weight: 400;
`

export const LabelX = `
  color: var(--marine-blue);
  font-size: 0.75rem;
  /* font-size: clamp(2.5rem, 1.62rem + 3.76vw, 5rem); */
  font-weight: 400;
`

export const InputX = `
  color: var(--marine-blue);
  font-size: 0.9375rem;
  /* font-size: clamp(2.5rem, 1.62rem + 3.76vw, 5rem); */
  font-weight: 500;
`

export const ButtonX = `
  color: var(--white);
  font-size: 0.875rem;
  /* font-size: clamp(2.5rem, 1.62rem + 3.76vw, 5rem); */
  font-weight: 500;
`

export const FlexCenterSpace = `
  display: flex;
  align-items: center;
  justify-content: space-between;
`
